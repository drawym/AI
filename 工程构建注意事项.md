# 目前这个为模板工程，可根据用户需求进行修改

## 1. 工程结构

工程工具配置为:
    Cmake + GCC + ninja + jlink


## 2. 注意事项
    用户需要配置
        1. 配置cmake的环境变量
        2. 配置jlink的环境变量
        3. 配置ninja的环境变量
        4. 配置gcc的环境变量

    并且在本工程的script目录下，配置rtt-config.json文件
    然后ai就可以直接调用jlink_rtt.ps1脚本，来启动jlink，执行编译和下载动作


